{
  "name": "baseline-guard-action",
  "version": "0.1.0",
  "description": "GitHub Action to enforce Web Platform Baseline compatibility",
  "main": "dist/index.js",
  "scripts": {
    "build": "rimraf dist && ncc build src/index.js --license licenses.txt && copyfiles -u 1 node_modules/web-features/data.json dist/",
    "lint": "eslint . --ext .js,.css"
  },
  "keywords": [
    "github-action",
    "baseline",
    "web-features",
    "compatibility"
  ],
  "author": "Tayyib Ismail",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.0",
    "glob": "^10.2.0",
    "web-features": "^2.0.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.34.0",
    "rimraf": "^5.0.0",
    "copyfiles": "^2.4.1",
    "eslint": "^8.0.0"
    }
}